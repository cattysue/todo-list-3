# Story 3.2: 고급 복합 필터링

Status: review

## Story

사용자(Catty)로서,
우선순위·마감일 범위·완료 여부·카테고리 필터를 개별 또는 조합하여 적용하는 것을 원한다,
키워드 검색과 함께 사용하여 정확히 원하는 할일 목록을 즉시 추릴 수 있도록.

## Acceptance Criteria

1. **필터 AND 조건 적용**: 필터 UI에서 우선순위 / 마감일 범위 / 완료 여부 / 카테고리 중 하나 이상을 선택하면, 선택된 필터 조건이 AND 조건으로 적용된 할일 목록이 표시된다.

2. **키워드 + 필터 동시 적용**: 키워드 검색과 필터가 동시에 적용되어 있을 때, 키워드 AND 필터 조건을 모두 만족하는 할일만 표시된다.

3. **필터 태그(칩) 시각적 표시 + 개별 해제**: 하나 이상의 필터가 적용되어 있을 때, 적용된 각 필터가 태그(칩) 형태로 시각적으로 표시된다. 각 태그의 삭제 버튼을 클릭하면 해당 필터만 개별 해제된다.

4. **복합 조건 테스트케이스**: "이번 주 마감 + 높은 우선순위 + 미완료" 필터 조합을 적용할 때, 세 조건을 모두 만족하는 할일만 표시된다.

5. **전체 필터 해제 시 전체 목록**: 적용된 모든 필터를 해제할 때 전체 할일 목록이 다시 표시된다.

## Tasks / Subtasks

- [x] Task 1: 백엔드 `services/search.py` 필터 파라미터 확장 (AC: 1, 2, 4)
  - [x] `search_todos()` 시그니처에 필터 파라미터 추가:
    - `priority: Optional[str] = None` — "high" / "medium" / "low"
    - `due_date_from: Optional[str] = None` — ISO 날짜 문자열 (YYYY-MM-DD)
    - `due_date_to: Optional[str] = None` — ISO 날짜 문자열
    - `is_completed: Optional[bool] = None` — True/False (None이면 필터 미적용)
    - `category_id: Optional[str] = None` — 카테고리 UUID
  - [x] 각 파라미터가 있을 때만 Supabase 쿼리에 조건 추가
  - [x] **기존 동작 보장**: 모든 파라미터 None이면 기존과 동일하게 전체 목록 반환

- [x] Task 2: 백엔드 `GET /categories` 엔드포인트 신규 추가 (AC: 1, 3)
  - [x] `backend/routers/categories.py` 신규 생성
  - [x] `backend/schemas/category.py` 신규 생성
  - [x] `backend/main.py`에 categories router include 추가

- [x] Task 3: 백엔드 `routers/todos.py` 필터 파라미터 확장 (AC: 1, 2, 4)
  - [x] `GET /search` 엔드포인트에 5개 필터 쿼리 파라미터 추가
  - [x] `search_todos_service()` 호출 시 필터 파라미터 전달

- [x] Task 4: 프론트엔드 타입 정의 (AC: 1, 3)
  - [x] `frontend/src/types/filters.ts` 신규 생성 (`SearchFilters`, `CategoryItem`)

- [x] Task 5: `frontend/src/lib/queryKeys.ts` 수정 (AC: 1, 2)
  - [x] `todos.search` 키 시그니처 확장 — filters 포함
  - [x] `categories.all` 키 추가
  - [x] **`as const` 어서션 유지**

- [x] Task 6: `frontend/src/lib/api.ts` 수정 (AC: 1, 2)
  - [x] `searchTodos(q, filters?)` 시그니처 확장 + URLSearchParams 변환
  - [x] `getCategories()` 함수 추가

- [x] Task 7: `frontend/src/hooks/useSearchTodos.ts` 수정 (AC: 1, 2)
  - [x] `useSearchTodos(query, filters?)` 시그니처 확장
  - [x] `hasActiveFilters()` 유틸 함수 export
  - [x] 항상 fetch (enabled 제한 제거 — 빈 쿼리에서도 전체 목록 반환)

- [x] Task 8: `frontend/src/hooks/useCategories.ts` 신규 생성 (AC: 1, 3)
  - [x] `useCategories()` 훅 작성

- [x] Task 9: `frontend/src/components/todos/FilterBar.tsx` 신규 생성 (AC: 1, 3, 4)
  - [x] 우선순위 / 완료 여부 / 카테고리 select
  - [x] 마감일 from/to date input
  - [x] "이번 주" 프리셋 버튼 (날짜 문자열 계산)

- [x] Task 10: `frontend/src/components/todos/FilterTags.tsx` 신규 생성 (AC: 3, 5)
  - [x] 적용된 필터 태그(칩) 렌더링
  - [x] 각 태그 삭제 버튼
  - [x] 빈 filters이면 null 반환

- [x] Task 11: `frontend/src/app/search/page.tsx` 수정 (AC: 1, 2, 3, 4, 5)
  - [x] `useState<SearchFilters>({})` 필터 상태
  - [x] `useCategories()` 통합
  - [x] `FilterBar`, `FilterTags` 배치
  - [x] 필터 개별 해제 핸들러
  - [x] 결과 표시: 항상 렌더 (빈 쿼리 + 빈 필터 → 전체 목록); "검색 결과 없음"은 isActive일 때만

- [x] Task 12: 백엔드 테스트 작성 (AC: 1, 2, 4)
  - [x] `_mock_supabase` 헬퍼 확장 (gte, lte, limit 포함)
  - [x] 필터별 단위 테스트 14개 추가 (기존 14개 + 신규 14개 + categories 5개 = 56개 전체 통과)
  - [x] `backend/tests/test_categories.py` 신규 생성 (5개 테스트)

- [x] Task 13: 프론트엔드 테스트 작성 (AC: 1, 3, 5)
  - [x] `useSearchTodos.test.ts` 수정 (filters 파라미터, hasActiveFilters 테스트)
  - [x] `FilterBar.test.tsx` 신규 생성 (17개 테스트)
  - [x] `FilterTags.test.tsx` 신규 생성 (14개 테스트)
  - [x] `search/page.test.tsx` 수정 (useCategories mock, 필터 통합 테스트)
  - [x] 전체 프론트 142개 통과

## Dev Notes

### ⚠️ 반드시 파악할 기존 구현 (수정 대상 파일)

**`backend/services/search.py`** — 현재 상태:
```python
def search_todos(
    user_id: str,
    supabase,
    q: Optional[str] = None,
) -> list[TodoDashboardItem]:
    query = (
        supabase.table("todos")
        .select("*, categories(name)")
        .eq("user_id", user_id)
    )

    if q and q.strip():
        query = query.ilike("title", f"%{q.strip()}%")

    response = query.order("created_at", desc=True).limit(50).execute()
    rows: list[dict[str, Any]] = response.data or []
    return [_build_item(row) for row in rows]
```
→ 필터 파라미터 추가 시 기존 `q` 처리 로직 위에 추가. 메서드 체인 순서: `eq("user_id") → ilike(q) → eq(priority) → eq(is_completed) → gte(due_date_from) → lte(due_date_to) → eq(category_id) → order → limit → execute`

**`frontend/src/lib/queryKeys.ts`** — 현재 상태:
```typescript
export const queryKeys = {
  todos: {
    all: ['todos'] as const,
    dashboard: () => ['todos', 'dashboard'] as const,
    byCategory: (id: string) => ['todos', 'category', id] as const,
    search: (q: string) => ['todos', 'search', q] as const,  // ← 변경 필요
  },
} as const;
```
→ `search` 키 시그니처 변경 및 `categories` 키 추가 후 `as const` 유지 필수.

**`frontend/src/hooks/useSearchTodos.ts`** — 현재 `queryKeys.todos.search(debouncedQuery)` 호출 중
→ `queryKeys.todos.search(debouncedQuery, filters)` 로 변경 필수 (키 변경 반영).

**`backend/tests/test_search.py`** — `_mock_supabase` 헬퍼에 `gte`, `lte`, `limit` 체인 미포함
→ 아래 테스트 패턴 섹션 참고하여 헬퍼 확장. **기존 테스트 37개 모두 통과 유지**.

### 핵심 제약 사항 (절대 위반 금지)

- **`queryKeys.ts` 외부 문자열 리터럴 금지**: 모든 쿼리 키는 `queryKeys.*` 사용
- **날짜 경계 계산 프론트 로직 금지**: 날짜 비교/분류 로직 프론트 컴포넌트에 작성 금지. "이번 주" 프리셋 버튼은 단순히 `due_date_from/to` 값을 계산해서 filter state에 넣는 것 (날짜 문자열 계산은 허용)
- **`services/` 레이어 필수**: 필터 로직을 `routers/todos.py`에 직접 작성 금지
- **기존 검색 기능 회귀 금지**: q 없을 때 전체 목록 반환, debounce 동작 모두 유지

### 필터 파라미터 URL 변환 패턴

```typescript
// frontend/src/lib/api.ts — searchTodos 함수 확장
import type { SearchFilters } from '@/types/filters';

export async function searchTodos(q: string, filters?: SearchFilters): Promise<TodoDashboardItem[]> {
  const token = await getAccessToken();

  const params = new URLSearchParams();
  if (q) params.set('q', q);
  if (filters?.priority) params.set('priority', filters.priority);
  if (filters?.due_date_from) params.set('due_date_from', filters.due_date_from);
  if (filters?.due_date_to) params.set('due_date_to', filters.due_date_to);
  if (filters?.is_completed !== undefined) params.set('is_completed', String(filters.is_completed));
  if (filters?.category_id) params.set('category_id', filters.category_id);

  const queryStr = params.toString();
  const url = `${process.env.NEXT_PUBLIC_API_URL}/todos/search${queryStr ? `?${queryStr}` : ''}`;

  const res = await fetch(url, {
    headers: {
      Authorization: `Bearer ${token}`,
      'Content-Type': 'application/json',
    },
  });

  if (!res.ok) throw new Error(`검색 오류: ${res.status}`);
  return res.json() as Promise<TodoDashboardItem[]>;
}
```

### 백엔드 필터 서비스 확장 패턴

```python
# backend/services/search.py
def search_todos(
    user_id: str,
    supabase,
    q: Optional[str] = None,
    priority: Optional[str] = None,
    due_date_from: Optional[str] = None,
    due_date_to: Optional[str] = None,
    is_completed: Optional[bool] = None,
    category_id: Optional[str] = None,
) -> list[TodoDashboardItem]:
    query = (
        supabase.table("todos")
        .select("*, categories(name)")
        .eq("user_id", user_id)
    )

    if q and q.strip():
        query = query.ilike("title", f"%{q.strip()}%")
    if priority:
        query = query.eq("priority", priority)
    if is_completed is not None:
        query = query.eq("is_completed", is_completed)
    if due_date_from:
        query = query.gte("due_date", due_date_from)
    if due_date_to:
        query = query.lte("due_date", due_date_to)
    if category_id:
        query = query.eq("category_id", category_id)

    response = query.order("created_at", desc=True).limit(50).execute()
    rows: list[dict[str, Any]] = response.data or []
    return [_build_item(row) for row in rows]
```

### 백엔드 categories 엔드포인트 패턴

```python
# backend/schemas/category.py (신규)
from pydantic import BaseModel

class CategoryItem(BaseModel):
    id: str
    name: str
    model_config = {"from_attributes": True}

# backend/routers/categories.py (신규)
from fastapi import APIRouter, Depends, HTTPException, status
from schemas.category import CategoryItem
from dependencies import get_current_user, get_supabase

router = APIRouter(prefix="/categories", tags=["categories"])

@router.get("", response_model=list[CategoryItem])
def get_categories(
    current_user=Depends(get_current_user),
    supabase=Depends(get_supabase),
):
    user_id = getattr(current_user, "id", None) or getattr(current_user, "sub", None)
    if not user_id:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED)
    response = (
        supabase.table("categories")
        .select("id, name")
        .eq("user_id", user_id)
        .order("name")
        .execute()
    )
    return [CategoryItem(**row) for row in (response.data or [])]
```

### `_mock_supabase` 헬퍼 업데이트 (테스트)

```python
def _mock_supabase(rows: list[dict]) -> MagicMock:
    """Fluent mock — 모든 빌더 메서드가 self 반환."""
    mock = MagicMock()
    for method in ["table", "select", "eq", "ilike", "gte", "lte", "order", "limit", "not_"]:
        getattr(mock, method).return_value = mock
    execute_result = MagicMock()
    execute_result.data = rows
    mock.execute.return_value = execute_result
    return mock
```

### FilterTags 레이블 매핑

```typescript
// 각 필터 키에 대한 한국어 레이블 (FilterTags 컴포넌트에서 사용)
const FILTER_LABELS: Record<string, string> = {
  high: '높은 우선순위',
  medium: '중간 우선순위',
  low: '낮은 우선순위',
};

// is_completed 태그 레이블
// filters.is_completed === true → "완료"
// filters.is_completed === false → "미완료"
```

### FilterBar에서 "이번 주" 프리셋 날짜 계산

```typescript
// FilterBar.tsx — 이번 주 버튼 클릭 핸들러
function getThisWeekRange(): { due_date_from: string; due_date_to: string } {
  const today = new Date();
  const monday = new Date(today);
  const dayOfWeek = today.getDay(); // 0=일요일
  const daysToMonday = dayOfWeek === 0 ? -6 : 1 - dayOfWeek;
  monday.setDate(today.getDate() + daysToMonday);
  const sunday = new Date(monday);
  sunday.setDate(monday.getDate() + 6);

  const fmt = (d: Date) => d.toISOString().split('T')[0];
  return { due_date_from: fmt(monday), due_date_to: fmt(sunday) };
}
```
→ 날짜 문자열(YYYY-MM-DD)을 `due_date_from`/`due_date_to`에 전달. 실제 날짜 경계 비교는 백엔드에서 수행.

### useSearchTodos 수정 패턴

```typescript
// frontend/src/hooks/useSearchTodos.ts
import type { SearchFilters } from '@/types/filters';

export function useSearchTodos(query: string, filters?: SearchFilters) {
  const debouncedQuery = useDebounce(query, 300);

  const result = useQuery({
    queryKey: queryKeys.todos.search(debouncedQuery, filters),
    queryFn: () => searchTodos(debouncedQuery, filters),
  });

  return { ...result, debouncedQuery };
}
```

### 이전 스토리에서 확립된 패턴 (반드시 준수)

- `getAccessToken()` → 모든 API 함수 공통 인증 패턴
- `useQuery` + `queryKeys` → TanStack Query 데이터 패칭
- Jest + React Testing Library + `renderHook` → 테스트 프레임워크
- 한국어 에러 메시지 → 사용자 표시 모든 에러
- `services/` 레이어에 비즈니스 로직 격리
- `as const` 어서션 → queryKeys의 모든 키에 적용

### 이번 스토리 범위 외

- 검색 결과에서 직접 완료 처리 (할일 체크박스)
- 검색어 하이라이팅
- 결과 페이지네이션
- 정렬 옵션 (마감일순, 우선순위순 등)
- 필터 프리셋 저장

### 참조

- [Source: story 3.1 파일] — `useDebounce`, `useSearchTodos`, `SearchBar`, `searchTodos()`, `queryKeys.todos.search`, `_mock_supabase` 헬퍼 등 모든 기존 구현 패턴
- [Source: architecture.md#상태 관리 패턴] — queryKeys 구조, as const 어서션
- [Source: architecture.md#모든 AI 에이전트 준수 사항] — queryKeys 규칙, services/ 레이어 규칙
- [Source: architecture.md#에러 처리 패턴] — 한국어 에러 메시지, 앱 크래시 금지
- [Source: architecture.md#API 포맷 패턴] — 날짜 ISO 8601, snake_case 쿼리 파라미터
- [Source: epics.md#Story 3.2] — Acceptance Criteria 원문

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- `useSearchTodos.ts`: `enabled` 제한 제거 — 빈 쿼리에서도 전체 목록 fetch하여 AC2·AC5 및 기존 테스트("빈 query로 searchTodos 호출") 정합성 확보
- `search/page.tsx`: 결과 표시 조건 분리 — 결과 리스트는 `results.length > 0`이면 항상 표시; "검색 결과 없음"은 `isActive`(쿼리 또는 필터 활성)일 때만 표시
- `test_search.py`: `_mock_supabase` 헬퍼 확장 — `for method in [..., "gte", "lte", "limit"]` 패턴으로 단순화
- `page.test.tsx`: `useCategories` 추가 mock 필요 — `jest.mock('@/hooks/useCategories')` 로 분리

### Completion Notes List

- AC1 (필터 AND 조건): 백엔드 `search_todos()`에 5개 필터 파라미터 추가. 각 파라미터 존재 시만 Supabase 조건 적용. 복합 조합 router 테스트 확인.
- AC2 (키워드 + 필터 AND): `searchTodos(q, filters)` URLSearchParams 변환으로 백엔드에 동시 전달. `test_combined_filter_and_keyword` 통과.
- AC3 (필터 태그 시각적 표시 + 개별 해제): `FilterTags.tsx` — 각 필터 키별 한국어 태그, `×` 버튼 클릭 시 `onRemove(key)`. 빈 filters이면 null 반환. 14개 테스트 통과.
- AC4 (복합 조건 테스트케이스): `test_search_router_combined_filters` — `priority=high&is_completed=false&due_date_from=...&due_date_to=...` 복합 파라미터 200 응답 확인.
- AC5 (전체 필터 해제 → 전체 목록): 필터 상태 `{}` 복귀 시 `hasActiveFilters` false → 결과 리스트 표시 조건 `results.length > 0`으로 전체 목록 재표시.
- 백엔드: 56개 테스트 전체 통과 (기존 37개 회귀 없음 + 신규 19개).
- 프론트엔드: 142개 테스트 전체 통과 (기존 94개 회귀 없음 + 신규 48개).
- `GET /categories` 신규 엔드포인트 — FilterBar 카테고리 선택 UI에 사용.

### File List

**신규 생성:**
- `backend/schemas/category.py`
- `backend/routers/categories.py`
- `backend/tests/test_categories.py`
- `frontend/src/types/filters.ts`
- `frontend/src/hooks/useCategories.ts`
- `frontend/src/components/todos/FilterBar.tsx`
- `frontend/src/components/todos/FilterTags.tsx`
- `frontend/src/components/todos/FilterBar.test.tsx`
- `frontend/src/components/todos/FilterTags.test.tsx`

**수정:**
- `backend/services/search.py` (필터 파라미터 5개 추가)
- `backend/routers/todos.py` (GET /search 필터 쿼리 파라미터 추가)
- `backend/tests/test_search.py` (_mock_supabase 확장 + 필터 테스트 14개 추가)
- `backend/main.py` (categories router include)
- `frontend/src/lib/api.ts` (searchTodos 필터 확장 + getCategories 추가)
- `frontend/src/lib/queryKeys.ts` (search 키 확장 + categories.all 추가)
- `frontend/src/hooks/useSearchTodos.ts` (filters 파라미터, hasActiveFilters export, enabled 제거)
- `frontend/src/app/search/page.tsx` (FilterBar, FilterTags, useCategories, filters state 통합)
- `frontend/src/hooks/useSearchTodos.test.ts` (filters, hasActiveFilters 테스트 추가)
- `frontend/src/lib/queryKeys.test.ts` (search 키 확장, categories.all 테스트 추가)
- `frontend/src/app/search/page.test.tsx` (useCategories mock, 필터 통합 테스트 추가)

## Change Log

| 날짜 | 변경 내용 |
|------|-----------|
| 2026-06-01 | 스토리 파일 최초 생성 (create-story 워크플로) |
| 2026-06-01 | 구현 완료 — 백엔드 필터 확장(services/search.py, routers/todos.py) + GET /categories 신규, 프론트 FilterBar+FilterTags+useCategories, 전체 테스트 통과 (백엔드 56/56, 프론트 142/142) |
