# Story 1.4: 대시보드에서 할일 완료 처리

Status: done

## Story

사용자(Catty)로서,
대시보드의 각 할일 항목에서 직접 완료 체크를 하는 것을 원한다,
카테고리 뷰로 이동하지 않고도 그 자리에서 바로 할일을 처리할 수 있도록.

## Acceptance Criteria

1. **낙관적 업데이트**: 대시보드 어떤 섹션에서든 완료 체크박스를 클릭하면 해당 항목이 즉시 해당 섹션에서 사라진다 (낙관적 업데이트). 완료 처리 API 호출은 백그라운드에서 진행된다.

2. **카테고리 뷰 동기화**: 완료 처리 API 호출이 성공하면 `queryKeys.todos.all` 캐시가 무효화되어 카테고리 뷰도 완료 상태로 갱신된다.

3. **롤백 및 에러 메시지**: 완료 처리 중 네트워크 오류가 발생하면 낙관적 업데이트가 롤백되어 해당 할일이 섹션에 다시 표시된다. 한국어 에러 메시지가 표시된다.

4. **체크박스 비활성화**: 완료 처리 mutation이 진행 중일 때 해당 체크박스가 비활성화되어 중복 클릭이 방지된다.

## Tasks / Subtasks

- [x] Task 1: 백엔드 완료 처리 엔드포인트 구현 (AC: 1, 2)
  - [x] `backend/routers/todos.py` 신규 생성: `PATCH /todos/{todo_id}` 엔드포인트 추가
  - [x] 요청 바디: `{ "is_completed": true }` (Pydantic 스키마 `TodoUpdateRequest`)
  - [x] 사용자 소유권 검증 필수 (본인 할일만 수정 가능)
  - [x] 성공 응답: 업데이트된 할일 객체 또는 `{ "id": todo_id, "is_completed": true }`
  - [x] 인증 실패 시 `401`, 본인 것이 아닌 경우 `404`, 이미 완료된 경우에도 `200` 반환
  - [x] `backend/main.py`에 todos 라우터 등록

- [x] Task 2: `backend/schemas/todo.py` 신규 생성 (AC: 1)
  - [x] `TodoUpdateRequest(BaseModel)`: `is_completed: bool` 필드
  - [x] `TodoUpdateResponse(BaseModel)`: `id: str`, `is_completed: bool` 필드

- [x] Task 3: `frontend/src/lib/api.ts` 수정 — completeTodo 함수 추가 (AC: 1, 2)
  - [x] `completeTodo(todoId: string): Promise<void>` 함수 추가
  - [x] Supabase 세션에서 `access_token` 추출 후 `Authorization: Bearer {token}` 헤더 포함 (기존 `getDashboardTodos` 패턴 그대로 재사용)
  - [x] `PATCH ${process.env.NEXT_PUBLIC_API_URL}/todos/${todoId}` 호출
  - [x] 요청 바디: `JSON.stringify({ is_completed: true })`
  - [x] 비정상 응답 시 에러 throw

- [x] Task 4: `frontend/src/hooks/useCompleteTodo.ts` 신규 생성 (AC: 1~4)
  - [x] `useMutation` + `useQueryClient` 사용 (TanStack Query)
  - [x] `mutationFn`: `completeTodo(todoId)` 호출
  - [x] `onMutate`: 낙관적 업데이트 — 대시보드 캐시에서 해당 todoId 항목 즉시 제거
  - [x] `onError`: 이전 캐시 상태로 롤백 + 한국어 에러 메시지 (`window.alert` 또는 console.error — 알림 라이브러리 미도입 시 alert 사용)
  - [x] `onSuccess`: `queryClient.invalidateQueries({ queryKey: queryKeys.todos.all })` 호출
  - [x] `isPending`, `variables` 반환 — 체크박스 비활성화에 사용

- [x] Task 5: 섹션 컴포넌트 수정 — 체크박스 UI 추가 (AC: 1, 4)
  - [x] `components/dashboard/DashboardSection.tsx` 수정:
    - `onComplete?: (id: string) => void` prop 추가
    - `completingId?: string | null` prop 추가 (mutation 중인 todo ID)
    - 각 `<li>` 항목에 `<input type="checkbox">` 추가
    - `completingId === item.id` 일 때 `disabled` 속성 적용
  - [x] `components/dashboard/OverdueSection.tsx` 수정:
    - `onComplete`, `completingId` props 추가 및 전달
  - [x] `TodaySection.tsx`, `TomorrowSection.tsx`, `WeekSection.tsx`: `DashboardSection`에 `onComplete`, `completingId` 전달

- [x] Task 6: `app/dashboard/page.tsx` 수정 — 훅 연결 (AC: 1~4)
  - [x] `useCompleteTodo` import 및 사용
  - [x] `const { mutate: completeTodo, isPending, variables } = useCompleteTodo()`
  - [x] 각 섹션 컴포넌트에 `onComplete={completeTodo}`, `completingId={isPending ? variables : null}` 전달

- [x] Task 7: 테스트 작성 (AC: 1~4)
  - [x] `hooks/useCompleteTodo.test.ts` 신규:
    - 성공 시 대시보드 캐시에서 아이템 제거 확인
    - 성공 시 `queryKeys.todos.all` invalidation 확인
    - 실패 시 캐시 롤백 확인
  - [x] `components/dashboard/DashboardSection.test.tsx` 업데이트:
    - 체크박스 렌더링 확인
    - 체크박스 클릭 시 `onComplete` 호출 확인
    - `completingId` 일치 시 체크박스 `disabled` 확인
  - [x] `app/dashboard/page.test.tsx` 업데이트:
    - 체크박스 클릭 시 아이템 즉시 사라짐 확인 (낙관적 업데이트 통합 테스트)

## Review Findings

### Decision Needed

- [x] [Review][Decision] 동시 뮤테이션 허용 여부 — `isPending` 중 모든 체크박스 비활성화로 결정. `disabled={!!completingId}` 적용.
- [x] [Review][Decision] `checked={false}` 하드코딩 — `checked={completingId === item.id}`로 변경하여 진행 중 시각 피드백 표시로 결정.

### Patches

- [x] [Review][Patch] `onSettled` 미지정 대시보드 무효화 제거 — onSuccess 후 onSettled가 dashboard를 재무효화하여 이중 refetch 발생; onError 후 onSettled가 롤백을 경쟁 조건으로 덮어씀 [frontend/src/hooks/useCompleteTodo.ts:51-53]
- [x] [Review][Patch] 백엔드 응답이 DB 행 대신 요청 페이로드 반환 — DB 트리거/정책으로 값이 변경될 경우 잘못된 성공 응답 전달 [backend/routers/todos.py:32]
- [x] [Review][Patch] `onError` 롤백 — 캐시 cold 상태 시 롤백 건너뜀 — `previousData`가 undefined이면 제거된 항목이 복원되지 않고 영구 소실 [frontend/src/hooks/useCompleteTodo.ts:37-45]
- [x] [Review][Patch] `todo_id` UUID 검증 없음 — 경로 파라미터가 임의 문자열 허용 [backend/routers/todos.py:9]

### Deferred

- [x] [Review][Defer] `getUser()` + `getSession()` TOCTOU — 두 호출 사이 세션 만료 가능성. 기존 `getDashboardTodos`와 동일한 패턴 [frontend/src/lib/api.ts] — deferred, pre-existing
- [x] [Review][Defer] 낙관적 업데이트 시 `DashboardResponse` 알 수 없는 필드 드롭 — 4개 키만 재구성하여 미래 필드 소실 가능성 [frontend/src/hooks/useCompleteTodo.ts:20-31] — deferred, pre-existing
- [x] [Review][Defer] `NEXT_PUBLIC_API_URL` 트레일링 슬래시 가드 없음 — 기존 `getDashboardTodos`와 동일한 패턴 [frontend/src/lib/api.ts] — deferred, pre-existing
- [x] [Review][Defer] `OverdueSection` category_name 표시 / `DashboardSection` 미표시 UI 불일치 — Story 1.3 기존 설계 [frontend/src/components/dashboard/OverdueSection.tsx] — deferred, pre-existing

## Dev Notes

### ⚠️ 반드시 파악할 기존 구현 (수정 대상 파일)

**`frontend/src/lib/api.ts`** — 현재 `getDashboardTodos()` 하나만 존재. 완전히 읽고 패턴 확인 후 `completeTodo` 추가:
```typescript
// 기존 패턴 (getDashboardTodos와 동일하게):
// 1. supabase.auth.getUser() → 미인증 시 에러 throw
// 2. supabase.auth.getSession() → access_token 추출
// 3. fetch() with Authorization: Bearer {token}

export async function completeTodo(todoId: string): Promise<void> {
  const supabase = createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) throw new Error('인증이 필요합니다.');
  const { data: { session } } = await supabase.auth.getSession();
  if (!session?.access_token) throw new Error('인증이 필요합니다.');
  
  const res = await fetch(
    `${process.env.NEXT_PUBLIC_API_URL}/todos/${todoId}`,
    {
      method: 'PATCH',
      headers: {
        Authorization: `Bearer ${session.access_token}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ is_completed: true }),
    },
  );
  if (!res.ok) throw new Error(`완료 처리 오류: ${res.status}`);
}
```

**`frontend/src/app/dashboard/page.tsx`** — 현재 상태:
```tsx
// 4개 섹션 컴포넌트 + useDashboard() 사용 중
// OverdueSection, TodaySection, TomorrowSection, WeekSection에 items만 전달
// → useCompleteTodo 추가 후 onComplete, completingId props도 전달해야 함
```

**`frontend/src/components/dashboard/DashboardSection.tsx`** — 현재 상태:
```typescript
interface DashboardSectionProps {
  title: string;
  items: TodoDashboardItem[];
  emptyMessage?: string;
  titleClassName?: string;
}
// → onComplete, completingId props 추가 필요
```

**`frontend/src/components/dashboard/OverdueSection.tsx`** — 현재 상태:
```tsx
// DashboardSection을 사용하지 않고 직접 렌더링
// → onComplete, completingId props 추가 필요
```

### 핵심 제약 사항 (절대 위반 금지)

- **`queryKeys.todos.all` 무효화 필수**: 완료 처리 성공 후 반드시 `invalidateQueries({ queryKey: queryKeys.todos.all })` 호출. 누락 시 카테고리 뷰와 대시보드가 동기화되지 않음 (FR-5 핵심 요구사항).
- **`queryKeys.ts` 외부 문자열 리터럴 금지**: `useCompleteTodo.ts`에서 `['todos']` 직접 사용 금지 → 반드시 `queryKeys.todos.all` 사용.
- **날짜 재분류 금지**: 낙관적 업데이트 시 프론트에서 아이템 재분류 금지. 섹션에서 ID로 필터링만 수행.
- **기존 테스트 유지**: `page.test.tsx`의 기존 11개 테스트 모두 통과 유지 필수. 체크박스 관련 테스트 추가만.

### 낙관적 업데이트 구현 패턴

```typescript
// frontend/src/hooks/useCompleteTodo.ts
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { completeTodo } from '@/lib/api';
import { queryKeys } from '@/lib/queryKeys';
import type { DashboardResponse } from '@/types/dashboard';

export function useCompleteTodo() {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (todoId: string) => completeTodo(todoId),
    
    onMutate: async (todoId: string) => {
      await queryClient.cancelQueries({ queryKey: queryKeys.todos.dashboard() });
      const previousData = queryClient.getQueryData<DashboardResponse>(queryKeys.todos.dashboard());
      queryClient.setQueryData<DashboardResponse>(queryKeys.todos.dashboard(), (old) => {
        if (!old) return old;
        const removeItem = (items: typeof old.overdue) => items.filter(i => i.id !== todoId);
        return {
          overdue: removeItem(old.overdue),
          today: removeItem(old.today),
          tomorrow: removeItem(old.tomorrow),
          this_week: removeItem(old.this_week),
        };
      });
      return { previousData };
    },
    
    onError: (_err, _todoId, context) => {
      if (context?.previousData) {
        queryClient.setQueryData(queryKeys.todos.dashboard(), context.previousData);
      }
      alert('할일 완료 처리 중 오류가 발생했습니다. 다시 시도해 주세요.');
    },
    
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: queryKeys.todos.all });
    },
    
    onSettled: () => {
      queryClient.invalidateQueries({ queryKey: queryKeys.todos.dashboard() });
    },
  });
}
```

### 백엔드 엔드포인트 패턴

```python
# backend/routers/todos.py (신규)
from fastapi import APIRouter, Depends, HTTPException, status
from schemas.todo import TodoUpdateRequest, TodoUpdateResponse
from dependencies import get_current_user, get_supabase

router = APIRouter(prefix="/todos", tags=["todos"])

@router.patch("/{todo_id}", response_model=TodoUpdateResponse)
async def update_todo(
    todo_id: str,
    body: TodoUpdateRequest,
    current_user=Depends(get_current_user),
    supabase=Depends(get_supabase),
):
    user_id = getattr(current_user, "id", None) or getattr(current_user, "sub", None)
    if not user_id:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED)
    
    result = (
        supabase.table("todos")
        .update({"is_completed": body.is_completed})
        .eq("id", todo_id)
        .eq("user_id", user_id)
        .execute()
    )
    
    if not result.data:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="할일을 찾을 수 없습니다.")
    
    return TodoUpdateResponse(id=todo_id, is_completed=body.is_completed)
```

```python
# backend/schemas/todo.py (신규)
from pydantic import BaseModel

class TodoUpdateRequest(BaseModel):
    is_completed: bool

class TodoUpdateResponse(BaseModel):
    id: str
    is_completed: bool
```

```python
# backend/main.py 수정 — todos 라우터 추가
from routers import dashboard, todos

app.include_router(dashboard.router)
app.include_router(todos.router)  # ← 추가
```

### 체크박스 UI 패턴

**`DashboardSection.tsx` 업데이트:**
```tsx
interface DashboardSectionProps {
  title: string;
  items: TodoDashboardItem[];
  emptyMessage?: string;
  titleClassName?: string;
  onComplete?: (id: string) => void;     // 추가
  completingId?: string | null;           // 추가 — mutation 중인 todo ID
}

// 렌더링 시:
<li key={item.id} className="py-1 border-b border-gray-100 flex items-center gap-2">
  <input
    type="checkbox"
    checked={false}
    disabled={completingId === item.id}
    onChange={() => onComplete?.(item.id)}
    aria-label={`${item.title} 완료 처리`}
    className="w-4 h-4 cursor-pointer disabled:opacity-50 disabled:cursor-not-allowed"
  />
  <span>{item.title}</span>
</li>
```

**`page.tsx` 수정:**
```tsx
import { useCompleteTodo } from '@/hooks/useCompleteTodo';

export default function DashboardPage() {
  const { data, isLoading, isError } = useDashboard();
  const { mutate: completeTodo, isPending, variables } = useCompleteTodo();
  
  const completingId = isPending ? (variables ?? null) : null;
  // ...
  // 각 섹션에:
  <TodaySection
    items={today}
    onComplete={completeTodo}
    completingId={completingId}
  />
```

### Story 1.3 완료 확인 사항 (이미 존재하는 것들)

Story 1.3에서 구현 완료:
- `frontend/src/components/dashboard/DashboardSection.tsx` — 공통 섹션 래퍼
- `frontend/src/components/dashboard/OverdueSection.tsx` — 기한 초과 (직접 렌더링, DashboardSection 미사용)
- `frontend/src/components/dashboard/TodaySection.tsx` — DashboardSection 사용
- `frontend/src/components/dashboard/TomorrowSection.tsx` — DashboardSection 사용
- `frontend/src/components/dashboard/WeekSection.tsx` — DashboardSection 사용
- `frontend/src/app/dashboard/page.tsx` — 4개 섹션 컴포넌트 + useDashboard() 사용
- `frontend/src/hooks/useDashboard.ts` — read query 훅

**이번 스토리 범위**: 체크박스 UI 추가 + 완료 mutation 훅 + 백엔드 엔드포인트. 기존 표시 로직 유지.

### 이번 스토리 범위 외

- 완료 후 성공 토스트 알림 (Story 1.4는 alert 또는 에러 메시지만)
- 할일 수정/삭제 기능
- 체크 애니메이션 효과

### 테스트 프레임워크 (Story 1.2~1.3에서 확립됨)

- Jest + React Testing Library
- 테스트 파일: 컴포넌트/훅 옆 co-located
- 훅 테스트: `renderHook` + `QueryClient` wrapper 사용
- `page.test.tsx`의 기존 11개 테스트 모두 유지 필수

### 참조

- [Source: architecture.md#상태 관리 패턴] — `queryKeys.todos.all` 무효화, 낙관적 업데이트 패턴
- [Source: architecture.md#모든 AI 에이전트 준수 사항] — queryKeys 규칙, 완료 처리 후 todos.all 무효화 필수
- [Source: architecture.md#에러 처리 패턴] — 한국어 에러 메시지, 앱 크래시 금지
- [Source: epics.md#Story 1.4] — Acceptance Criteria 원문
- [Source: implementation-artifacts/1-3-시간-섹션-컴포넌트-구현.md] — 이전 스토리 파일 목록 및 패턴

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- `page.test.tsx` 업데이트: `useCompleteTodo` 모킹 추가 (`jest.mock('@/hooks/useCompleteTodo')`) — page.tsx가 이제 해당 훅을 사용하므로 모킹 필수
- `useCompleteTodo.test.ts`: `isPending` 진행 중 테스트에서 `Promise`를 직접 제어하는 패턴 사용 (resolve 함수 외부 노출)

### Completion Notes List

- AC1: `useCompleteTodo` 훅 — `onMutate`에서 대시보드 캐시를 즉시 필터링. 체크박스 클릭 시 즉각 UI 반영. 테스트: `useCompleteTodo.test.ts` 5/5 통과.
- AC2: `onSuccess`에서 `queryClient.invalidateQueries({ queryKey: queryKeys.todos.all })` 호출. 테스트: invalidation 확인.
- AC3: `onError`에서 `context.previousData`로 캐시 복원 + `alert('할일 완료 처리 중 오류가 발생했습니다. 다시 시도해 주세요.')` 표시. 테스트: 롤백 및 알림 확인.
- AC4: `isPending && variables === item.id` 조건으로 체크박스 `disabled` 처리. 테스트: `DashboardSection.test.tsx`, `page.test.tsx` 각각 확인.
- 전체 테스트: 60/60 통과 (9개 테스트 suite). 회귀 없음.
- `onSettled`에서 대시보드 쿼리도 무효화하여 서버 최신 상태 보장.
- 백엔드: `PATCH /todos/{todo_id}` — 소유권 검증(`user_id` 매칭), `404` 반환 구현.

### File List

**신규 생성:**
- `backend/routers/todos.py`
- `backend/schemas/todo.py`
- `frontend/src/hooks/useCompleteTodo.ts`
- `frontend/src/hooks/useCompleteTodo.test.ts`

**수정:**
- `backend/main.py` (todos 라우터 등록)
- `frontend/src/lib/api.ts` (completeTodo 함수 추가)
- `frontend/src/components/dashboard/DashboardSection.tsx` (onComplete, completingId props + 체크박스 UI)
- `frontend/src/components/dashboard/OverdueSection.tsx` (onComplete, completingId props + 체크박스 UI)
- `frontend/src/components/dashboard/TodaySection.tsx` (props 전달)
- `frontend/src/components/dashboard/TomorrowSection.tsx` (props 전달)
- `frontend/src/components/dashboard/WeekSection.tsx` (props 전달)
- `frontend/src/app/dashboard/page.tsx` (useCompleteTodo 연결)
- `frontend/src/components/dashboard/DashboardSection.test.tsx` (체크박스 테스트 5개 추가)
- `frontend/src/app/dashboard/page.test.tsx` (useCompleteTodo 모킹 + 체크박스 테스트 2개 추가)

## Change Log

| 날짜 | 변경 내용 |
|------|-----------|
| 2026-06-01 | 스토리 파일 최초 생성 (create-story 워크플로) |
| 2026-06-01 | 구현 완료 — 백엔드 PATCH 엔드포인트, useCompleteTodo 훅, 체크박스 UI, 테스트 60/60 통과 |
