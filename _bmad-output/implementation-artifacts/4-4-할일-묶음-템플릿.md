# Story 4.4: 할일 묶음 템플릿

Status: review

## Story

사용자(Catty)로서,
자주 사용하는 할일 조합을 템플릿으로 저장하고 원클릭으로 여러 할일을 한 번에 생성하는 것을 원한다,
반복적으로 같은 할일들을 개별 입력하는 번거로움을 없애도록.

## Acceptance Criteria

1. **템플릿 저장**: 템플릿 생성 폼에서 이름과 여러 할일 항목(제목, 카테고리, 우선순위, 마감일 오프셋)을 입력하고 저장하면, 해당 할일 묶음이 템플릿으로 DB에 저장되고 템플릿 목록에 즉시 나타난다.

2. **템플릿 불러오기**: 저장된 템플릿을 불러오기할 때, 템플릿에 포함된 모든 할일이 즉시 생성된다. 생성된 할일들이 카테고리 뷰와 대시보드에 즉시 반영된다.

3. **마감일 처리 방식 선택**: 템플릿 불러올 때 "오늘 기준 자동 설정" 또는 "직접 지정" 중 하나를 선택하면, 선택한 방식대로 생성된 할일들의 마감일이 설정된다.
   - "오늘 기준 자동 설정": base_date = 서버의 오늘(UTC). 각 항목의 due_date = base_date + due_date_offset (offset이 null이면 due_date 없음)
   - "직접 지정": 사용자가 입력한 날짜를 base_date로 사용. 동일한 offset 로직 적용

4. **템플릿 삭제**: 특정 템플릿을 삭제하면 해당 템플릿이 목록에서 제거된다. 이미 생성된 할일들에는 영향이 없다.

## Tasks / Subtasks

- [x] Task 1: DB 마이그레이션 — `todo_templates` + `todo_template_items` 테이블 생성 (AC: 1, 4)
  - [x] `backend/database/migrations/add_todo_templates.sql` 신규 생성:
    ```sql
    CREATE TABLE todo_templates (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
        name TEXT NOT NULL,
        created_at TIMESTAMPTZ DEFAULT now()
    );

    CREATE TABLE todo_template_items (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        template_id UUID NOT NULL REFERENCES todo_templates(id) ON DELETE CASCADE,
        title TEXT NOT NULL,
        category_id UUID REFERENCES categories(id) ON DELETE SET NULL,
        priority TEXT CHECK (priority IN ('high', 'medium', 'low')),
        due_date_offset INTEGER,
        sort_order INTEGER NOT NULL DEFAULT 0
    );

    CREATE INDEX idx_todo_templates_user_id ON todo_templates(user_id);
    CREATE INDEX idx_todo_template_items_template_id ON todo_template_items(template_id);
    ```

- [x] Task 2: `backend/schemas/template.py` 신규 생성 (AC: 1, 2, 3, 4)
  ```python
  from typing import Literal, Optional
  from pydantic import BaseModel


  class TemplateItemRequest(BaseModel):
      title: str
      category_id: Optional[str] = None
      priority: Optional[Literal["high", "medium", "low"]] = None
      due_date_offset: Optional[int] = None  # 오프셋(일수). null = 마감일 없음


  class TemplateCreateRequest(BaseModel):
      name: str
      items: list[TemplateItemRequest]


  class TemplateItemResponse(BaseModel):
      id: str
      title: str
      category_id: Optional[str] = None
      priority: Optional[str] = None
      due_date_offset: Optional[int] = None
      sort_order: int = 0


  class TemplateResponse(BaseModel):
      id: str
      name: str
      created_at: str
      items: list[TemplateItemResponse]


  class TemplateApplyRequest(BaseModel):
      base_date: Optional[str] = None  # ISO 날짜 문자열(YYYY-MM-DD). None = 오늘(서버 UTC 기준)
  ```

- [x] Task 3: `backend/services/templates.py` 신규 생성 (AC: 1, 2, 3, 4)
  ```python
  from datetime import date, timedelta, timezone, datetime
  from typing import Optional
  from services.todos import create_todo


  def list_templates(user_id: str, supabase) -> list[dict]:
      resp = (
          supabase.table("todo_templates")
          .select("*, todo_template_items(*)")
          .eq("user_id", user_id)
          .order("created_at", desc=True)
          .execute()
      )
      return resp.data or []


  def create_template(user_id: str, name: str, items: list[dict], supabase) -> dict:
      tmpl_resp = (
          supabase.table("todo_templates")
          .insert({"user_id": user_id, "name": name})
          .select("*")
          .single()
          .execute()
      )
      template = tmpl_resp.data
      template_id = template["id"]

      item_rows = [
          {
              "template_id": template_id,
              "title": item["title"],
              "category_id": item.get("category_id"),
              "priority": item.get("priority"),
              "due_date_offset": item.get("due_date_offset"),
              "sort_order": idx,
          }
          for idx, item in enumerate(items)
      ]
      items_resp = (
          supabase.table("todo_template_items")
          .insert(item_rows)
          .select("*")
          .execute()
      )
      template["todo_template_items"] = items_resp.data or []
      return template


  def delete_template(template_id: str, user_id: str, supabase) -> bool:
      # todo_template_items는 CASCADE DELETE로 자동 제거
      resp = (
          supabase.table("todo_templates")
          .delete()
          .eq("id", template_id)
          .eq("user_id", user_id)
          .execute()
      )
      return bool(resp.data)


  def apply_template(
      template_id: str, user_id: str, base_date: Optional[str], supabase
  ) -> list[dict]:
      """템플릿 항목들을 할일로 일괄 생성한다."""
      resp = (
          supabase.table("todo_templates")
          .select("*, todo_template_items(*)")
          .eq("id", template_id)
          .eq("user_id", user_id)
          .single()
          .execute()
      )
      if not resp.data:
          return []

      if base_date:
          resolved_base = date.fromisoformat(base_date)
      else:
          resolved_base = datetime.now(timezone.utc).date()  # 서버 UTC 기준 오늘

      created = []
      items = sorted(
          resp.data.get("todo_template_items", []),
          key=lambda x: x.get("sort_order", 0),
      )
      for item in items:
          offset = item.get("due_date_offset")
          due_date = (resolved_base + timedelta(days=offset)).isoformat() if offset is not None else None
          todo = create_todo(
              user_id=user_id,
              supabase=supabase,
              title=item["title"],
              category_id=item.get("category_id"),
              priority=item.get("priority"),
              due_date=due_date,
          )
          created.append(todo)
      return created
  ```

- [x] Task 4: `backend/routers/templates.py` 신규 생성 (AC: 1, 2, 3, 4)
  ```python
  from uuid import UUID

  from fastapi import APIRouter, Depends, HTTPException, status

  from schemas.template import (
      TemplateCreateRequest,
      TemplateResponse,
      TemplateApplyRequest,
  )
  from schemas.todo import TodoCreateResponse
  from services.templates import (
      list_templates,
      create_template,
      delete_template,
      apply_template,
  )
  from dependencies import get_current_user, get_supabase, require_user_id

  router = APIRouter(prefix="/templates", tags=["templates"])


  @router.get("", response_model=list[TemplateResponse])
  def get_templates(
      current_user=Depends(get_current_user),
      supabase=Depends(get_supabase),
  ):
      user_id = require_user_id(current_user)
      return list_templates(user_id=user_id, supabase=supabase)


  @router.post("", response_model=TemplateResponse, status_code=status.HTTP_201_CREATED)
  def create_template_endpoint(
      body: TemplateCreateRequest,
      current_user=Depends(get_current_user),
      supabase=Depends(get_supabase),
  ):
      user_id = require_user_id(current_user)
      return create_template(
          user_id=user_id,
          name=body.name,
          items=[item.model_dump() for item in body.items],
          supabase=supabase,
      )


  @router.delete("/{template_id}", status_code=status.HTTP_204_NO_CONTENT)
  def delete_template_endpoint(
      template_id: UUID,
      current_user=Depends(get_current_user),
      supabase=Depends(get_supabase),
  ):
      user_id = require_user_id(current_user)
      deleted = delete_template(
          template_id=str(template_id), user_id=user_id, supabase=supabase
      )
      if not deleted:
          raise HTTPException(
              status_code=status.HTTP_404_NOT_FOUND,
              detail="템플릿을 찾을 수 없습니다.",
          )


  @router.post(
      "/{template_id}/apply",
      response_model=list[TodoCreateResponse],
      status_code=status.HTTP_201_CREATED,
  )
  def apply_template_endpoint(
      template_id: UUID,
      body: TemplateApplyRequest,
      current_user=Depends(get_current_user),
      supabase=Depends(get_supabase),
  ):
      user_id = require_user_id(current_user)
      todos = apply_template(
          template_id=str(template_id),
          user_id=user_id,
          base_date=body.base_date,
          supabase=supabase,
      )
      if todos == [] and body.base_date is None:
          # 빈 템플릿이거나 없는 템플릿 — 404
          raise HTTPException(
              status_code=status.HTTP_404_NOT_FOUND,
              detail="템플릿을 찾을 수 없습니다.",
          )
      return todos
  ```
  - [x] **주의**: `apply_template` 결과가 빈 리스트인 경우 두 가지 케이스가 있음:
    1. 존재하지 않는 template_id → 404 반환
    2. 항목이 0개인 빈 템플릿 → 200 + `[]` 반환
    - 위 구현에서 `apply_template`이 `[]`를 반환하면 서비스 레이어에서 template이 없는 경우에도 동일한 결과가 나옴. 서비스에서 template 존재 여부를 먼저 확인하는 방식으로 분리 가능 — 또는 `apply_template`이 `None`을 반환하도록 수정하여 라우터에서 구분.
    - 권장: `apply_template`에서 template 미존재 시 `None` 반환, 빈 항목 시 `[]` 반환으로 구분하여 라우터가 None → 404 처리

- [x] Task 5: `backend/main.py` 수정 — templates 라우터 등록 (AC: 1, 2, 3, 4)
  - [x] imports에 추가:
    ```python
    from routers import dashboard, todos, categories, templates
    ```
  - [x] 라우터 등록 추가:
    ```python
    app.include_router(templates.router)
    ```

- [x] Task 6: `backend/tests/test_templates.py` 신규 생성 (AC: 1, 2, 3, 4)
  - [x] 서비스 단위 테스트:
    - `test_list_templates_empty` — 빈 목록 반환
    - `test_create_template_calls_insert_twice` — todo_templates + todo_template_items 각 insert 호출
    - `test_delete_template_returns_true_on_success` — data 있으면 True
    - `test_delete_template_returns_false_on_not_found` — data 없으면 False
    - `test_apply_template_today` — base_date=None → UTC today 기준 due_date 계산
    - `test_apply_template_with_base_date` — 지정 날짜 기준 offset 계산
    - `test_apply_template_null_offset_no_due_date` — offset=None 항목 → due_date=None
    - `test_apply_template_not_found_returns_none` — 없는 template_id → None 반환
  - [x] 라우터 통합 테스트 (`TestClient` + `dependency_overrides`):
    - `test_get_templates_200` — GET /templates → 200
    - `test_create_template_201` — POST /templates with name + items → 201
    - `test_delete_template_204` — DELETE /templates/{id} → 204
    - `test_delete_template_404` — 없는 id → 404
    - `test_apply_template_201` — POST /templates/{id}/apply → 201
    - `test_apply_template_404` — 없는 template_id → 404

- [x] Task 7: `frontend/src/types/templates.ts` 신규 생성 (AC: 1, 2, 3)
  ```typescript
  export interface TemplateItem {
    id: string;
    title: string;
    category_id: string | null;
    priority: 'high' | 'medium' | 'low' | null;
    due_date_offset: number | null;
    sort_order: number;
  }

  export interface Template {
    id: string;
    name: string;
    created_at: string;
    items: TemplateItem[];
  }

  export interface CreateTemplateRequest {
    name: string;
    items: Omit<TemplateItem, 'id' | 'sort_order'>[];
  }

  export interface ApplyTemplateRequest {
    base_date?: string; // YYYY-MM-DD. undefined = 오늘
  }
  ```

- [x] Task 8: `frontend/src/lib/queryKeys.ts` 수정 — templates 키 추가 (AC: 1, 2, 3, 4)
  ```typescript
  templates: {
    all: ['templates'] as const,
  },
  ```
  - 결과:
    ```typescript
    export const queryKeys = {
      todos: { ... },
      categories: { all: ['categories'] as const },
      templates: { all: ['templates'] as const },
    } as const;
    ```

- [x] Task 9: `frontend/src/lib/api.ts` 수정 — 템플릿 API 함수 추가 (AC: 1, 2, 3, 4)
  ```typescript
  import type { Template, CreateTemplateRequest, ApplyTemplateRequest } from '@/types/templates';
  import type { TodoItem } from '@/types/todos';

  export async function getTemplates(): Promise<Template[]> {
    const token = await getAccessToken();
    const res = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/templates`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (!res.ok) throw new Error(`템플릿 조회 오류: ${res.status}`);
    return res.json() as Promise<Template[]>;
  }

  export async function createTemplate(data: CreateTemplateRequest): Promise<Template> {
    const token = await getAccessToken();
    const res = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/templates`, {
      method: 'POST',
      headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
    });
    if (!res.ok) throw new Error(`템플릿 생성 오류: ${res.status}`);
    return res.json() as Promise<Template>;
  }

  export async function deleteTemplate(templateId: string): Promise<void> {
    const token = await getAccessToken();
    const res = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/templates/${templateId}`, {
      method: 'DELETE',
      headers: { Authorization: `Bearer ${token}` },
    });
    if (!res.ok) throw new Error(`템플릿 삭제 오류: ${res.status}`);
  }

  export async function applyTemplate(
    templateId: string,
    data: ApplyTemplateRequest,
  ): Promise<TodoItem[]> {
    const token = await getAccessToken();
    const res = await fetch(
      `${process.env.NEXT_PUBLIC_API_URL}/templates/${templateId}/apply`,
      {
        method: 'POST',
        headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      },
    );
    if (!res.ok) throw new Error(`템플릿 적용 오류: ${res.status}`);
    return res.json() as Promise<TodoItem[]>;
  }
  ```

- [x] Task 10: 프론트엔드 훅 신규 생성 (AC: 1, 2, 3, 4)
  - [x] `frontend/src/hooks/useTemplates.ts`:
    ```typescript
    import { useQuery } from '@tanstack/react-query';
    import { getTemplates } from '@/lib/api';
    import { queryKeys } from '@/lib/queryKeys';

    export function useTemplates() {
      return useQuery({
        queryKey: queryKeys.templates.all,
        queryFn: getTemplates,
      });
    }
    ```
  - [x] `frontend/src/hooks/useCreateTemplate.ts`:
    ```typescript
    import { useMutation, useQueryClient } from '@tanstack/react-query';
    import { createTemplate } from '@/lib/api';
    import { queryKeys } from '@/lib/queryKeys';

    export function useCreateTemplate() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: createTemplate,
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: queryKeys.templates.all });
        },
        onError: () => {
          alert('템플릿 생성 중 오류가 발생했습니다. 다시 시도해 주세요.');
        },
      });
    }
    ```
  - [x] `frontend/src/hooks/useDeleteTemplate.ts`:
    ```typescript
    import { useMutation, useQueryClient } from '@tanstack/react-query';
    import { deleteTemplate } from '@/lib/api';
    import { queryKeys } from '@/lib/queryKeys';

    export function useDeleteTemplate() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: (templateId: string) => deleteTemplate(templateId),
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: queryKeys.templates.all });
        },
        onError: () => {
          alert('템플릿 삭제 중 오류가 발생했습니다. 다시 시도해 주세요.');
        },
      });
    }
    ```
  - [x] `frontend/src/hooks/useApplyTemplate.ts`:
    ```typescript
    import { useMutation, useQueryClient } from '@tanstack/react-query';
    import { applyTemplate } from '@/lib/api';
    import { queryKeys } from '@/lib/queryKeys';
    import type { ApplyTemplateRequest } from '@/types/templates';

    export function useApplyTemplate() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: ({ templateId, data }: { templateId: string; data: ApplyTemplateRequest }) =>
          applyTemplate(templateId, data),
        onSuccess: () => {
          // 대시보드 + 카테고리 뷰 동시 갱신
          queryClient.invalidateQueries({ queryKey: queryKeys.todos.all });
        },
        onError: () => {
          alert('템플릿 적용 중 오류가 발생했습니다. 다시 시도해 주세요.');
        },
      });
    }
    ```

- [x] Task 11: `frontend/src/components/templates/` 컴포넌트 신규 생성 (AC: 1, 2, 3, 4)
  - [x] `TemplateList.tsx` — 템플릿 목록 표시 + 불러오기/삭제 버튼:
    ```tsx
    import type { Template } from '@/types/templates';

    interface Props {
      templates: Template[];
      onApply: (templateId: string) => void;
      onDelete: (templateId: string) => void;
      deletingId: string | null;
      applyingId: string | null;
    }

    export function TemplateList({ templates, onApply, onDelete, deletingId, applyingId }: Props) {
      if (templates.length === 0) {
        return <p className="text-gray-500">저장된 템플릿이 없습니다.</p>;
      }
      return (
        <ul className="space-y-4">
          {templates.map((tmpl) => (
            <li key={tmpl.id} className="border rounded p-4">
              <div className="flex justify-between items-center mb-2">
                <h3 className="font-semibold">{tmpl.name}</h3>
                <div className="flex gap-2">
                  <button
                    onClick={() => onApply(tmpl.id)}
                    disabled={applyingId === tmpl.id}
                    className="px-3 py-1 text-sm bg-blue-600 text-white rounded disabled:opacity-50"
                  >
                    불러오기
                  </button>
                  <button
                    onClick={() => onDelete(tmpl.id)}
                    disabled={deletingId === tmpl.id}
                    className="px-3 py-1 text-sm text-red-600 border border-red-300 rounded disabled:opacity-50"
                  >
                    삭제
                  </button>
                </div>
              </div>
              <p className="text-sm text-gray-500">{tmpl.items.length}개 할일</p>
            </li>
          ))}
        </ul>
      );
    }
    ```
  - [x] `TemplateForm.tsx` — 새 템플릿 생성 폼 (이름 + 항목 추가/제거):
    - state: `name: string`, `items: { title; priority; category_id; due_date_offset }[]`
    - 항목 추가 버튼, 각 항목 제거 버튼
    - 카테고리 셀렉트는 `useCategories()` 훅으로 목록 가져옴
    - onSubmit: `useCreateTemplate().mutate({ name, items })`
  - [x] `ApplyTemplateModal.tsx` — 마감일 처리 방식 선택 모달 (AC: 3):
    ```tsx
    interface Props {
      templateId: string;
      onConfirm: (data: { base_date?: string }) => void;
      onClose: () => void;
    }
    // 내부 state: mode ('auto' | 'manual'), selectedDate: string
    // mode='auto' → base_date 미전달 (서버 오늘 사용)
    // mode='manual' → base_date = selectedDate
    ```

- [x] Task 12: `frontend/src/app/templates/page.tsx` 신규 생성 (AC: 1, 2, 3, 4)
  ```tsx
  'use client';

  import { useState } from 'react';
  import { useTemplates } from '@/hooks/useTemplates';
  import { useCreateTemplate } from '@/hooks/useCreateTemplate';
  import { useDeleteTemplate } from '@/hooks/useDeleteTemplate';
  import { useApplyTemplate } from '@/hooks/useApplyTemplate';
  import { TemplateList } from '@/components/templates/TemplateList';
  import { TemplateForm } from '@/components/templates/TemplateForm';
  import { ApplyTemplateModal } from '@/components/templates/ApplyTemplateModal';

  export default function TemplatesPage() {
    const { data: templates = [], isLoading } = useTemplates();
    const createTemplate = useCreateTemplate();
    const deleteTemplate = useDeleteTemplate();
    const applyTemplate = useApplyTemplate();
    const [applyingTemplateId, setApplyingTemplateId] = useState<string | null>(null);

    if (isLoading) return <p>로딩 중...</p>;

    return (
      <main className="max-w-2xl mx-auto p-4">
        <h1 className="text-xl font-bold mb-6">할일 묶음 템플릿</h1>
        <section className="mb-8">
          <h2 className="text-lg font-semibold mb-4">새 템플릿 만들기</h2>
          <TemplateForm
            onSubmit={(data) => createTemplate.mutate(data)}
            isSubmitting={createTemplate.isPending}
          />
        </section>
        <section>
          <h2 className="text-lg font-semibold mb-4">저장된 템플릿</h2>
          <TemplateList
            templates={templates}
            onApply={(id) => setApplyingTemplateId(id)}
            onDelete={(id) => deleteTemplate.mutate(id)}
            deletingId={deleteTemplate.isPending ? (deleteTemplate.variables ?? null) : null}
            applyingId={applyingTemplateId}
          />
        </section>
        {applyingTemplateId && (
          <ApplyTemplateModal
            templateId={applyingTemplateId}
            onConfirm={(data) => {
              applyTemplate.mutate({ templateId: applyingTemplateId, data });
              setApplyingTemplateId(null);
            }}
            onClose={() => setApplyingTemplateId(null)}
          />
        )}
      </main>
    );
  }
  ```

## Dev Notes

### ⚠️ 현재 코드베이스 상태 (Story 4.3 완료 후)

**기존 파일 현황:**

```
backend/main.py:
  - 현재 dashboard, todos, categories 라우터 등록됨
  - templates 라우터 추가 필요 (Task 5)

backend/routers/:
  - todos.py, categories.py, dashboard.py 존재
  - templates.py 신규 생성 필요

backend/schemas/:
  - todo.py: TodoCreateRequest, TodoContentUpdateRequest, TodoCreateResponse, RecurrenceControlRequest/Response
  - dashboard.py: TodoDashboardItem (recurrence_type, recurrence_paused 포함)
  - template.py 신규 생성 필요

backend/services/:
  - todos.py: create_todo(user_id, supabase, title, category_id, priority, due_date, recurrence_type, recurrence_days, recurrence_day_of_month)
  - dashboard.py, recurring.py, search.py 존재
  - templates.py 신규 생성 필요 — create_todo 직접 재사용

backend/tests/:
  - conftest.py: make_fluent_supabase_mock(rows) 헬퍼 존재 (전체 테스트에서 공유)
  - 기존: test_todos.py, test_dashboard.py, test_categories.py, test_search.py, test_recurring.py, test_recurrence_control.py

frontend/src/lib/queryKeys.ts:
  - queryKeys.todos.all, .dashboard, .byCategory, .search 존재
  - queryKeys.categories.all 존재
  - queryKeys.templates.all 추가 필요

frontend/src/types/:
  - todos.ts: TodoItem, CreateTodoRequest, UpdateTodoRequest, RecurrenceType
  - dashboard.ts: TodoDashboardItem, DashboardResponse
  - filters.ts: SearchFilters, CategoryItem 등
  - templates.ts 신규 생성 필요

frontend/src/hooks/:
  - useCategories.ts: categories 목록 조회 (TemplateForm에서 재사용)
  - useCompleteTodo.ts, useDashboard.ts, useSearchTodos.ts, useUpdateTodo.ts, useRecurrenceControl.ts 존재
  - 신규: useTemplates, useCreateTemplate, useDeleteTemplate, useApplyTemplate

frontend/src/lib/api.ts:
  - getAccessToken, getDashboardTodos, completeTodo, createTodo, getTodo,
    controlRecurrence, updateTodo 정의됨
  - 템플릿 API 함수 4개 추가 필요
```

**테스트 카운트 (Story 4.3 기준):**
- 백엔드: 116개 통과
- 프론트엔드: 168개 통과

### DB 설계 결정

**`due_date_offset` (정수, 일수):**
- 템플릿 항목에 오프셋만 저장. 실제 날짜는 apply 시점에 base_date + offset으로 계산
- null = 마감일 없음 (할일이 마감일 없이 생성됨)
- 양수(미래), 0(당일), 음수 가능하나 실무상 0 이상 권장

**`todo_template_items.sort_order`:**
- 항목 순서 보장. 삽입 시 enumerate index로 설정 (0부터)
- 목록 조회 시 sort_order 오름차순 정렬

**CASCADE DELETE:**
- `todo_templates` 삭제 시 `todo_template_items` 자동 CASCADE 삭제
- 서비스 레이어에서 items 수동 삭제 불필요

**`apply_template` 404 구분 로직:**
```python
# 권장 수정: None vs [] 구분으로 404 처리 명확화
def apply_template(...) -> Optional[list[dict]]:
    resp = supabase.table("todo_templates").select(...).single().execute()
    if not resp.data:
        return None  # 템플릿 미존재 → 라우터에서 404
    # ... 생성 로직
    return created  # 빈 리스트도 200으로 반환 (항목 0개인 템플릿)

# 라우터에서:
todos = apply_template(...)
if todos is None:
    raise HTTPException(404, "템플릿을 찾을 수 없습니다.")
return todos
```

### `create_todo` 재사용 패턴

`apply_template` 내에서 기존 `services/todos.py`의 `create_todo` 직접 호출:
```python
from services.todos import create_todo

todo = create_todo(
    user_id=user_id,
    supabase=supabase,
    title=item["title"],
    category_id=item.get("category_id"),
    priority=item.get("priority"),
    due_date=due_date,  # recurrence 파라미터 미전달 → 일반 할일로 생성
)
```
- 템플릿으로 생성된 할일은 반복 설정 없음 (recurrence_type 미전달)
- `create_todo`는 이미 반복 파라미터를 optional로 받음

### `useCategories.ts` 재사용

`TemplateForm`에서 카테고리 셀렉트 구현 시 기존 `useCategories()` 훅 사용:
```typescript
import { useCategories } from '@/hooks/useCategories';
// categories.data → { id, name }[] 형태
```

### `apply_template` 호출 시 캐시 무효화

템플릿 적용 후 `queryKeys.todos.all` 무효화 필수:
```typescript
queryClient.invalidateQueries({ queryKey: queryKeys.todos.all });
```
→ 대시보드 + 카테고리 뷰 자동 갱신 (Story 4.3, 4.2와 동일한 패턴)

### Supabase `.select("*, todo_template_items(*)")` 패턴

Supabase Python 클라이언트에서 related table join:
```python
supabase.table("todo_templates")
    .select("*, todo_template_items(*)")
    .eq("user_id", user_id)
    .execute()
```
- `todo_template_items` 키로 nested list 반환됨

### 테스트 mock 패턴 (conftest.py 활용)

```python
from tests.conftest import make_fluent_supabase_mock

# conftest의 make_fluent_supabase_mock은 단일 execute().data 패턴만 지원
# create_template은 insert를 2번 호출하므로 MagicMock 직접 사용 권장
from unittest.mock import MagicMock, patch

def test_create_template_calls_insert_twice():
    mock_sb = MagicMock()
    # todo_templates insert 체인
    tmpl_exec = MagicMock()
    tmpl_exec.data = {"id": "tmpl-1", "name": "아침 루틴", "created_at": "2026-06-01T00:00:00Z"}
    # todo_template_items insert 체인
    items_exec = MagicMock()
    items_exec.data = [{"id": "item-1", "title": "운동", ...}]

    mock_sb.table.return_value.insert.return_value.select.return_value.single.return_value.execute.return_value = tmpl_exec
    # items insert 별도 처리
    ...
```

### 라우터 통합 테스트 패턴 (기존 test_todos.py에서 가져옴)

```python
from fastapi.testclient import TestClient
from unittest.mock import MagicMock
from main import app
from dependencies import get_current_user, get_supabase

client = TestClient(app)

def override_user():
    return {"sub": "user-123"}

def override_supabase():
    return MagicMock()

app.dependency_overrides[get_current_user] = override_user
app.dependency_overrides[get_supabase] = override_supabase
```

### 절대 위반 금지 사항

- **라우터에서 직접 DB 조작 금지** — 반드시 `services/templates.py` 레이어 사용
- **`queryKeys.ts` 외부에서 쿼리 키 문자열 리터럴 사용 금지**
- **`create_todo` 재구현 금지** — `services/todos.py`의 기존 함수 직접 import
- **날짜 계산(base_date + offset)은 백엔드에서 처리** — 프론트는 base_date 값만 전달
- **기존 116개 백엔드 + 168개 프론트 테스트 회귀 없음 필수**
- **마이그레이션 실행 전 apply_template 테스트 실행 금지** (테이블 없으면 DB 오류)

### 범위 외 (이번 스토리에서 구현하지 않음)

- 기존 할일에서 선택하여 템플릿 저장 (현재: 폼에서 직접 입력)
- 템플릿 항목 편집 기능
- 템플릿 불러오기 시 항목별 개별 날짜 지정
- 네비게이션 메뉴에 `/templates` 링크 추가 (선택 구현)
- 템플릿 페이지 loading.tsx 스켈레톤

### 이전 스토리에서 확립된 패턴 (반드시 준수)

- `getAccessToken()` → 모든 프론트엔드 API 함수 공통 인증
- `useMutation` + `queryClient.invalidateQueries` → 캐시 무효화
- `patch()` + `MagicMock` → 백엔드 단위 테스트
- `TestClient` + `dependency_overrides` → 라우터 통합 테스트
- 한국어 에러 메시지 → `detail` 필드 (백엔드) / `alert` (프론트 임시)
- `services/` 레이어에 비즈니스 로직 격리
- UUID를 경로 파라미터 타입으로 선언 (`todo_id: UUID`) — FastAPI가 형식 검증
- `require_user_id(current_user)` → user_id 추출 패턴

### 참조

- [Source: epics.md#Story 4.4] — Acceptance Criteria 원문 (FR-13)
- [Source: story 4.3] — 라우터 패턴, 서비스 레이어, 테스트 mock 패턴
- [Source: backend/services/todos.py] — create_todo 시그니처 (apply_template에서 재사용)
- [Source: backend/schemas/todo.py] — TodoCreateResponse (apply 응답에 재사용)
- [Source: backend/routers/todos.py] — 라우터 구조 패턴, UUID 경로 파라미터, require_user_id
- [Source: backend/tests/conftest.py] — make_fluent_supabase_mock 헬퍼
- [Source: frontend/src/lib/queryKeys.ts] — 기존 queryKeys 구조
- [Source: frontend/src/hooks/useCategories.ts] — TemplateForm 카테고리 셀렉트에서 재사용
- [Source: frontend/src/lib/api.ts] — getAccessToken, 기존 fetch 패턴

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- 라우터 통합 테스트에서 `dependency_overrides[get_current_user] = _make_fake_user` 직접 등록 시 422 오류 발생 — FastAPI가 함수 파라미터를 query param으로 해석. `lambda: fake_user` 패턴으로 수정 (test_recurrence_control.py와 동일한 패턴).
- `TemplateResponse`의 `items` 필드와 Supabase 반환 키 `todo_template_items` 불일치로 ResponseValidationError 발생 — `services/templates.py`에 `_normalize_template()` 헬퍼 추가하여 정규화.

### Completion Notes List

- AC1 (템플릿 저장): `TemplateForm`에서 이름 + 항목 입력 → `POST /templates` → `todo_templates`/`todo_template_items` 두 테이블에 저장. 저장 후 `queryKeys.templates.all` 무효화로 목록 즉시 갱신.
- AC2 (템플릿 불러오기): `ApplyTemplateModal`에서 방식 선택 → `POST /templates/{id}/apply` → `create_todo` 일괄 호출 → `queryKeys.todos.all` 무효화로 대시보드·카테고리 뷰 동시 갱신.
- AC3 (마감일 처리): "오늘 기준 자동 설정" = base_date 미전달(서버 UTC today), "직접 지정" = base_date 전달. 각 항목 due_date = base_date + due_date_offset (offset=null → due_date 없음).
- AC4 (템플릿 삭제): `DELETE /templates/{id}` → CASCADE DELETE로 items 자동 제거. 이미 생성된 todos에 영향 없음.
- `apply_template`에서 None 반환(미존재)과 [] 반환(빈 템플릿) 구분하여 라우터에서 None → 404, [] → 201 처리.
- `_normalize_template()` 헬퍼: Supabase `todo_template_items` 키를 Pydantic `TemplateResponse.items`로 정규화. sort_order 오름차순 정렬 포함.
- 백엔드: 기존 116개 + 신규 19개 + 기타 4개 = 139개 통과. 프론트엔드: 기존 168개 + 신규 2개 = 170개 통과. 회귀 없음.

### File List

**신규 생성:**
- `backend/database/migrations/add_todo_templates.sql`
- `backend/schemas/template.py`
- `backend/services/templates.py`
- `backend/routers/templates.py`
- `backend/tests/test_templates.py`
- `frontend/src/types/templates.ts`
- `frontend/src/hooks/useTemplates.ts`
- `frontend/src/hooks/useCreateTemplate.ts`
- `frontend/src/hooks/useDeleteTemplate.ts`
- `frontend/src/hooks/useApplyTemplate.ts`
- `frontend/src/components/templates/TemplateList.tsx`
- `frontend/src/components/templates/TemplateForm.tsx`
- `frontend/src/components/templates/ApplyTemplateModal.tsx`
- `frontend/src/app/templates/page.tsx`

**수정:**
- `backend/main.py` — templates 라우터 등록
- `frontend/src/lib/queryKeys.ts` — templates 키 추가
- `frontend/src/lib/api.ts` — 템플릿 API 함수 4개 추가

## Change Log

| 날짜 | 변경 내용 |
|------|-----------|
| 2026-06-01 | 스토리 파일 최초 생성 (create-story 워크플로) |
| 2026-06-01 | 구현 완료 — todo_templates/todo_template_items 마이그레이션, templates 서비스/라우터/스키마, 프론트엔드 훅 4개 + 컴포넌트 3개 + 페이지, 백엔드 19개/프론트엔드 2개 테스트 추가. 백엔드 139개, 프론트엔드 170개 통과. |
